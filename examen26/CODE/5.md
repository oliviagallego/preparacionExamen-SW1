# EXAMEN PRUEBA — Sistemas Web I — PARTE PRÁCTICA (2 horas, papel)

## Contexto general

Se desea crear una aplicación web mínima con **Node.js + Express + EJS** para gestionar usuarios con roles. La aplicación tendrá un **panel de administración** accesible solo por usuarios con rol **admin**.

La base de datos será **en memoria** (un array u objeto en `app.js` o `database.js`). No es necesario persistir datos en fichero.

## Requisitos

1. **Puerto del servidor**
   La aplicación debe iniciar en el puerto indicado por la variable de entorno `PORT` o, si no existe, en el puerto **3000**.

2. **Panel de administración**
   Debe existir un panel accesible únicamente por usuarios con rol **admin** que permita:

   * Visualizar un listado de usuarios.
   * Ver para cada usuario: `username`, `role`, `last_login` (si no existe, mostrar “Never”).
   * Eliminar usuarios (excepto el propio admin).

3. **Login/Logout y control de acceso**

   * Debe existir una pantalla de login.
   * Al hacer login correctamente, debe actualizarse `last_login` con la fecha actual.
   * El usuario logueado debe considerarse el `currentUser` para controlar permisos.
   * Debe existir logout.

> Nota: No es obligatorio usar express-session (puede usarse una variable `currentUser` global), pero el control de acceso debe funcionar.

## Estructura del proyecto (se proporciona / se debe respetar)

```
prueba/
├── public/
│   ├── styles.css
│   └── client.js
├── views/
│   ├── header.ejs
│   ├── index.ejs
│   ├── login.ejs
│   └── admin.ejs
├── database.js
├── app.js
└── package.json
```

Dependencias mínimas:

* `express`
* `ejs`

---

# Tareas a completar

## Apartado 1 — Configuración del servidor (1 punto)

Completar: puerto, servidor, vistas, estáticos, parseo formularios.

## Apartado 2 — Login y logout (1,5 puntos)

Completar: `GET /login`, `POST /login`, `POST /logout`.

## Apartado 3 — Panel de administración (2 puntos)

Completar: `GET /admin` (solo admin), `admin.ejs` tabla con acciones.

## Apartado 4 — Eliminar usuarios (1,5 puntos)

Completar: `POST /admin/deleteUser` con restricciones (no borrar admin ni a sí mismo).

## Apartado 5 — Menú común (opcional / mejora)

Header común con enlaces según haya usuario y si es admin.

---

# CÓDIGO BASE PROPORCIONADO (NO COMPLETO)

## `package.json`

```json
{
  "name": "prueba",
  "version": "1.0.0",
  "main": "app.js",
  "type": "commonjs",
  "scripts": {
    "start": "node app.js"
  },
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^4.19.2"
  }
}
```

## `database.js`

```js
// Base de datos en memoria (OBJETO por username)
const users = {
  admin: { username: "admin", password: "admin123", role: "admin", last_login: null },
  ana:   { username: "ana",   password: "ana123",   role: "user",  last_login: null },
  pepe:  { username: "pepe",  password: "pepe123",  role: "user",  last_login: null }
};

module.exports = { users };
```

## `app.js`

```js
const express = require("express");
const path = require("path");
const database = require("./database");

// “Usuario actual” (NO session)
let currentUser = null;

const app = express();

// ⚠️ Arranca en 3000 fijo (esto NO cumple el requisito del PORT todavía)
const PORT = 3000;

// Motor de plantillas
app.set("view engine", "ejs");
app.set("views", path.join(__dirname, "views"));

// Middlewares básicos
app.use(express.urlencoded({ extended: false }));
app.use(express.static(path.join(__dirname, "public")));

// Variables para vistas (se pasan “a mano” en cada render)
function viewBase() {
  return { currentUser };
}

// Home
app.get("/", (req, res) => {
  return res.render("index", {
    ...viewBase(),
    title: "Home"
  });
});

// Login (pantalla)
app.get("/login", (req, res) => {
  return res.render("login", {
    ...viewBase(),
    title: "Login",
    error: null
  });
});

// Login (acción) — versión base (NO actualiza last_login, y control simple)
app.post("/login", (req, res) => {
  const { username, password } = req.body;

  const user = database.users[username];

  if (user && user.password === password) {
    currentUser = { username: user.username, role: user.role };
    return res.redirect("/");
  }

  return res.status(401).render("login", {
    ...viewBase(),
    title: "Login",
    error: "Credenciales incorrectas"
  });
});

// Logout — versión base (GET en vez de POST)
app.get("/logout", (req, res) => {
  currentUser = null;
  return res.redirect("/");
});

// Admin — versión base (NO restringe acceso)
app.get("/admin", (req, res) => {
  // Pasa usuarios como array para pintar fácil
  const users = Object.values(database.users);

  return res.render("admin", {
    ...viewBase(),
    title: "Panel admin",
    users
  });
});

// Borrar usuario — versión base (sin restricciones)
app.post("/admin/deleteUser", (req, res) => {
  const { username } = req.body;

  if (database.users[username]) {
    delete database.users[username];
  }

  return res.redirect("/admin");
});

// Servidor
app.listen(PORT, () => {
  console.log("Servidor iniciado en puerto " + PORT);
});
```

---

## `views/header.ejs`

```ejs
<header class="top">
  <a href="/">Home</a>

  <span class="sep">|</span>

  <% if (currentUser) { %>
    <span>Logueado: <strong><%= currentUser.username %></strong> (<%= currentUser.role %>)</span>

    <span class="sep">|</span>

    <a href="/admin">Admin</a>

    <span class="sep">|</span>

    <!-- Logout base (link GET) -->
    <a href="/logout">Logout</a>
  <% } else { %>
    <a href="/login">Login</a>
  <% } %>
</header>
<hr/>
```

## `views/index.ejs`

```ejs
<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <%- include("header", { currentUser }) %>

    <h1>Home</h1>

    <% if (!currentUser) { %>
      <p>No estás logueado.</p>
    <% } else { %>
      <p>Bienvenido, <%= currentUser.username %>.</p>
    <% } %>
  </body>
</html>
```

## `views/login.ejs`

```ejs
<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <%- include("header", { currentUser }) %>

    <h1>Login</h1>

    <% if (error) { %>
      <p class="error"><%= error %></p>
    <% } %>

    <form method="post" action="/login">
      <label>Username</label>
      <input name="username" />

      <label>Password</label>
      <input name="password" type="password" />

      <button type="submit">Entrar</button>
    </form>
  </body>
</html>
```

## `views/admin.ejs`

```ejs
<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <%- include("header", { currentUser }) %>

    <h1>Panel admin</h1>

    <table border="1" cellpadding="8">
      <thead>
        <tr>
          <th>Username</th>
          <th>Role</th>
          <th>Last Login</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <% users.forEach(u => { %>
          <tr>
            <td><%= u.username %></td>
            <td><%= u.role %></td>
            <td><%= u.last_login %></td>
            <td>
              <form method="post" action="/admin/deleteUser">
                <input type="hidden" name="username" value="<%= u.username %>"/>
                <button type="submit">Eliminar</button>
              </form>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </body>
</html>
```

---

## `public/styles.css`

```css
body { font-family: Arial, sans-serif; padding: 16px; }
.top { display: flex; gap: 10px; align-items: center; }
.sep { opacity: 0.5; }
.error { color: crimson; font-weight: bold; }
table { margin-top: 12px; }
```

## `public/client.js`

```js
// Vacío (por si quieres añadir JS de cliente)
console.log("client.js cargado");
```

